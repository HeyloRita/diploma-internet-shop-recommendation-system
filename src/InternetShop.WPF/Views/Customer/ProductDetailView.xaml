<UserControl x:Class="InternetShop.WPF.Views.Customer.ProductDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="20,14"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <DockPanel>
                <Button Style="{StaticResource OutlineButton}"
                        Content="← Назад в каталог"
                        Command="{Binding GoBackCommand}"
                        Padding="12,7" DockPanel.Dock="Left"/>
                <TextBlock Text="Карточка товара" FontSize="18" FontWeight="Bold"
                           Foreground="{StaticResource TextBrush}"
                           VerticalAlignment="Center" Margin="16,0,0,0"/>
            </DockPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="28,24,28,28">

                <StackPanel HorizontalAlignment="Center"
                            Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"
                            Margin="0,60">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="4"
                                 Foreground="{StaticResource PrimaryBrush}"/>
                </StackPanel>

                <StackPanel Visibility="{Binding IsBusy,
                                Converter={StaticResource InverseBoolToVisibility}}">

                    <Border Style="{StaticResource Card}" Margin="0,0,0,24">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="240"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Background="#F0F7FF" CornerRadius="8"
                                    Height="220" Margin="0,0,24,0">
                                <TextBlock Text="📦" FontSize="72"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Border>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Product.CategoryName}"
                                           FontSize="12" Foreground="{StaticResource MutedBrush}"
                                           Margin="0,0,0,6"/>
                                <TextBlock Text="{Binding Product.Name}"
                                           FontSize="22" FontWeight="Bold"
                                           Foreground="{StaticResource TextBrush}"
                                           TextWrapping="Wrap" Margin="0,0,0,12"/>
                                <TextBlock Text="{Binding Product.Description}"
                                           FontSize="14" Foreground="{StaticResource MutedBrush}"
                                           TextWrapping="Wrap" Margin="0,0,0,20"
                                           LineHeight="22"/>

                                <TextBlock Text="{Binding Product.Price, StringFormat={}{0:N0} ₽}"
                                           FontSize="28" FontWeight="Bold"
                                           Foreground="{StaticResource PrimaryBrush}"
                                           Margin="0,0,0,16"/>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                    <Border Background="#E8F5E9" CornerRadius="4" Padding="8,4"
                                            Visibility="{Binding Product.Stock,
                                                Converter={StaticResource StockToVisibility}}">
                                        <TextBlock Foreground="#2E7D32" FontSize="13">
                                            <Run Text="✓ В наличии: "/>
                                            <Run Text="{Binding Product.Stock}"/>
                                            <Run Text=" шт."/>
                                        </TextBlock>
                                    </Border>
                                    <Border Background="#FFEBEE" CornerRadius="4" Padding="8,4"
                                            Visibility="{Binding Product.Stock,
                                                Converter={StaticResource ZeroStockToVisibility}}">
                                        <TextBlock Text="✗ Нет в наличии"
                                                   Foreground="{StaticResource AccentBrush}"
                                                   FontSize="13"/>
                                    </Border>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal"
                                            Visibility="{Binding IsLoggedIn,
                                                Converter={StaticResource BoolToVisibility}}">
                                    <Border BorderBrush="{StaticResource BorderBrush}"
                                            BorderThickness="1" CornerRadius="6">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Style="{StaticResource OutlineButton}"
                                                    Content="−"
                                                    Command="{Binding DecreaseQtyCommand}"
                                                    Width="36" Padding="0"
                                                    BorderThickness="0"/>
                                            <TextBlock Text="{Binding Quantity}"
                                                       Width="40" TextAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       FontSize="15" FontWeight="SemiBold"/>
                                            <Button Style="{StaticResource OutlineButton}"
                                                    Content="+"
                                                    Command="{Binding IncreaseQtyCommand}"
                                                    Width="36" Padding="0"
                                                    BorderThickness="0"/>
                                        </StackPanel>
                                    </Border>
                                    <Button Style="{StaticResource PrimaryButton}"
                                            Content="Купить сейчас"
                                            Command="{Binding BuyCommand}"
                                            IsEnabled="{Binding IsBusy,
                                                Converter={StaticResource InverseBool}}"
                                            Padding="20,10" Margin="12,0,0,0"/>
                                </StackPanel>

                                <TextBlock Text="Войдите, чтобы оформить заказ."
                                           FontSize="13" Foreground="{StaticResource MutedBrush}"
                                           Margin="0,8,0,0"
                                           Visibility="{Binding IsLoggedIn,
                                               Converter={StaticResource InverseBoolToVisibility}}"/>

                                <Border CornerRadius="6" Padding="12,8" Margin="0,16,0,0"
                                        Visibility="{Binding StatusMessage,
                                            Converter={StaticResource StringToVisibility}}">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSuccess}" Value="True">
                                                    <Setter Property="Background" Value="#E8F5E9"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsSuccess}" Value="False">
                                                    <Setter Property="Background" Value="#FFEBEE"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding StatusMessage}"
                                               TextWrapping="Wrap" FontSize="13">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSuccess}" Value="True">
                                                        <Setter Property="Foreground" Value="#2E7D32"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsSuccess}" Value="False">
                                                        <Setter Property="Foreground" Value="#C62828"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <StackPanel Visibility="{Binding Recommendations.Count,
                                    Converter={StaticResource PositiveToVisibility}}">
                        <TextBlock Text="Вам может понравиться"
                                   FontSize="18" FontWeight="Bold"
                                   Foreground="{StaticResource TextBrush}"
                                   Margin="0,0,0,16"/>
                        <ItemsControl ItemsSource="{Binding Recommendations}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource Card}"
                                            Width="190" Margin="0,0,14,14"
                                            Cursor="Hand">
                                        <Border.InputBindings>
                                            <MouseBinding MouseAction="LeftClick"
                                                          Command="{Binding DataContext.OpenRecommendationCommand,
                                                              RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                          CommandParameter="{Binding}"/>
                                        </Border.InputBindings>
                                        <StackPanel>
                                            <Border Background="#F0F7FF" Height="100"
                                                    CornerRadius="6" Margin="0,0,0,10">
                                                <TextBlock Text="📦" FontSize="36"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="{Binding Name}"
                                                       FontSize="13" FontWeight="SemiBold"
                                                       TextWrapping="Wrap" MaxHeight="36"
                                                       TextTrimming="CharacterEllipsis"
                                                       Margin="0,0,0,6"/>
                                            <TextBlock Text="{Binding Price, StringFormat={}{0:N0} ₽}"
                                                       FontSize="14" FontWeight="Bold"
                                                       Foreground="{StaticResource PrimaryBrush}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

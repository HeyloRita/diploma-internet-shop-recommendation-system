<UserControl x:Class="InternetShop.WPF.Views.Customer.OrderHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="24,16"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <TextBlock Text="📋 История покупок" FontSize="22" FontWeight="Bold"
                       Foreground="{StaticResource TextBrush}"/>
        </Border>

        <Grid Grid.Row="1" Margin="20,16">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                        Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"
                             Foreground="{StaticResource PrimaryBrush}"/>
            </StackPanel>

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                        Visibility="{Binding StatusText,
                            Converter={StaticResource StringToVisibility}}">
                <TextBlock Text="📋" FontSize="48" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding StatusText}" FontSize="16"
                           Foreground="{StaticResource MutedBrush}"
                           HorizontalAlignment="Center" Margin="0,12,0,0"/>
            </StackPanel>

            <Grid Visibility="{Binding IsBusy,
                      Converter={StaticResource InverseBoolToVisibility}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="320"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Style="{StaticResource Card}" Padding="0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Orders}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="16,14" Cursor="Hand"
                                            BorderBrush="{StaticResource BorderBrush}"
                                            BorderThickness="0,0,0,1">
                                        <Border.InputBindings>
                                            <MouseBinding MouseAction="LeftClick"
                                                          Command="{Binding DataContext.SelectOrderCommand,
                                                              RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                          CommandParameter="{Binding}"/>
                                        </Border.InputBindings>
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#F5F9FF"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock FontSize="14" FontWeight="SemiBold"
                                                           Foreground="{StaticResource TextBrush}">
                                                    <Run Text="Заказ #"/>
                                                    <Run Text="{Binding Id}"/>
                                                </TextBlock>
                                                <TextBlock Text="{Binding CreatedAt, StringFormat={}{0:dd.MM.yyyy HH:mm}}"
                                                           FontSize="12"
                                                           Foreground="{StaticResource MutedBrush}"
                                                           Margin="0,2,0,0"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                                <TextBlock Text="{Binding TotalAmount, StringFormat={}{0:N0} ₽}"
                                                           FontSize="14" FontWeight="Bold"
                                                           Foreground="{StaticResource PrimaryBrush}"
                                                           HorizontalAlignment="Right"/>
                                                <Border CornerRadius="3" Padding="6,2"
                                                        HorizontalAlignment="Right" Margin="0,4,0,0">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="#E3F2FD"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status}" Value="Delivered">
                                                                    <Setter Property="Background" Value="#E8F5E9"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                                                                    <Setter Property="Background" Value="#FFEBEE"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <TextBlock Text="{Binding Status}"
                                                               FontSize="11"/>
                                                </Border>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource Card}"
                        Visibility="{Binding SelectedOrder,
                            Converter={StaticResource NullToVisibility}}">
                    <StackPanel>
                        <TextBlock FontSize="18" FontWeight="Bold"
                                   Foreground="{StaticResource TextBrush}" Margin="0,0,0,4">
                            <Run Text="Заказ #"/>
                            <Run Text="{Binding SelectedOrder.Id}"/>
                        </TextBlock>
                        <TextBlock Foreground="{StaticResource MutedBrush}" FontSize="13" Margin="0,0,0,20">
                            <Run Text="{Binding SelectedOrder.CreatedAt, StringFormat={}{0:dd MMMM yyyy, HH:mm}}"/>
                            <Run Text=" · "/>
                            <Run Text="{Binding SelectedOrder.Status}"/>
                        </TextBlock>

                        <ItemsControl ItemsSource="{Binding SelectedOrder.Items}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="0,10"
                                            BorderBrush="{StaticResource BorderBrush}"
                                            BorderThickness="0,0,0,1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="{Binding ProductName}"
                                                           FontSize="14" FontWeight="SemiBold"/>
                                                <TextBlock FontSize="12"
                                                           Foreground="{StaticResource MutedBrush}"
                                                           Margin="0,2,0,0">
                                                    <Run Text="{Binding Quantity}"/>
                                                    <Run Text=" × "/>
                                                    <Run Text="{Binding PriceAtOrder, StringFormat={}{0:N0} ₽}"/>
                                                </TextBlock>
                                            </StackPanel>
                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding LineTotal, StringFormat={}{0:N0} ₽}"
                                                       FontSize="14" FontWeight="Bold"
                                                       Foreground="{StaticResource PrimaryBrush}"
                                                       VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Border Padding="0,16,0,0">
                            <DockPanel>
                                <TextBlock Text="Итого:" FontSize="16" FontWeight="Bold"/>
                                <TextBlock DockPanel.Dock="Right"
                                           Text="{Binding SelectedOrder.TotalAmount, StringFormat={}{0:N0} ₽}"
                                           FontSize="18" FontWeight="Bold"
                                           Foreground="{StaticResource PrimaryBrush}"/>
                            </DockPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
